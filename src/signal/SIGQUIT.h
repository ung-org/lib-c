#include <signal.h>
#define SIGQUIT                                                              (3)
/*s Quit s*/
/* Value selected to match requirements for COMMAND(kill) and COMMAND(trap). */
/*
POSIX(1)
*/
