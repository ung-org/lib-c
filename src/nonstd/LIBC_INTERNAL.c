#include <nonstd/internal.h>

typedef enum {
	ERRNO,
	THREAD_LOCALE,
	GLOBAL_LOCALE,
	LOAD_LOCALE,
	SYSCALL_LOOKUP,
	PRINTF,
	SCANF,
	FOPEN,
	CTYPE,
	TOLOWER,
	TOUPPER,
	FILE_STREAMS,
	ATEXIT,
	RAND
} LIBC_INTERNAL;
